export class GlobalConstants {
    // Message
    public static genericError: string = "Somethimg went worng. Please try again later";

    // Regex
    public static emailRegex: string = "[A-Za-z0-9._%-]+@[A-Za-z0-9._%-]+\\.[a-z]{2,3}";
}
    